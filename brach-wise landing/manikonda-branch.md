okay now it's time for us to build new landing page for manikonda branch.
we already have an exsisting manikonda branch page and it's almost perfect.
we have to do some alterations. i have pasted the manikonda branch code in sap-manikonda.html 

we use this for both sap fico and mm course. so we need to have an separate js to handle them carefully.

 Cleanest fix: use a URL parameter (?course=fico or ?course=mm in your ad's Final URL) and a small script that swaps the H1, meta title, and the relevant module card to the top based on that parameter.


bbring all the images which every you need from the exsisting folder "GlobalCoach IT" to this folder. 
Recommended section order (same content, reordered for paid traffic)

Current → New:

Header — no change
Generic headline → keyword-specific headline (dynamic FICO/MM per point 2 above)
Hero + form — no change
Program Overview (module cards) → move up from position 9 to position 4, right after the hero — confirms "yes, this is the FICO/MM course you searched for" before anything else
Instant callback bar — no change
Alumni logos — no change
Testimonials — no change
Why Choose Us — no change
Mid-page form — no change
FAQ → move later, right before the final push — this is where you handle the fees/duration objections right before the last CTA, instead of it being sandwiched oddly mid-page
Re-engagement banner ("admissions open now") → last content section, immediately before footer
Footer — no change

The logic: message-match first (prove it's the right page), curriculum next (prove it's the right course), trust in the middle (testimonials/logos), objections handled late (FAQ), final urgency push right before they leave.